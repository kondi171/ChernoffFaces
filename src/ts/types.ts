export type ChernoffFace = {
  eyes: number,
  mouth: number,
  nose: number,
  eyebrows: number,
  head: number
}

export type VoivodeFace = {
  id: number,
  name: string,
  eyes: string | null,
  mouth: string | null,
  nose: string | null,
  eyebrows: string | null,
  head: string | null
};